# IBI_Investment_XIRR_Computation
This project takes as input transaction logs that have been downlowaded from https://sparkibi.ordernet.co.il/.
<br>
The output is the internal rate of return (XIRR).
